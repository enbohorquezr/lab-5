#include "Recursos.h"

Recursos::Recursos(Texture* _recursosTexture, int _posicionX, int _posicionY, int _ancho, int _alto, int _anchoPantalla, int _altoPantalla) :
	GameObject(_recursosTexture, _posicionX, _posicionY, _ancho, _alto, _anchoPantalla, _altoPantalla) {

};